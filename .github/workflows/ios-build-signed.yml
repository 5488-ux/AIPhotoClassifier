name: iOS Build (Signed - IPA)

on:
  workflow_dispatch:  # 手动触发
  push:
    tags:
      - 'v*'  # 当推送版本tag时触发

jobs:
  build-signed:
    runs-on: macos-14

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '15.2'

    - name: Install Apple Certificate and Provisioning Profile
      env:
        CERTIFICATES_P12: ${{ secrets.CERTIFICATES_P12 }}
        CERTIFICATES_P12_PASSWORD: ${{ secrets.CERTIFICATES_P12_PASSWORD }}
        PROVISIONING_PROFILE: ${{ secrets.PROVISIONING_PROFILE }}
        KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
      run: |
        # 创建临时目录
        CERTIFICATE_PATH=$RUNNER_TEMP/certificate.p12
        PP_PATH=$RUNNER_TEMP/profile.mobileprovision
        KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

        # 解码证书和Profile
        echo -n "$CERTIFICATES_P12" | base64 -D > $CERTIFICATE_PATH
        echo -n "$PROVISIONING_PROFILE" | base64 -D > $PP_PATH

        # 创建临时钥匙串
        security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
        security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
        security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

        # 导入证书
        security import $CERTIFICATE_PATH -P "$CERTIFICATES_P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
        security list-keychain -d user -s $KEYCHAIN_PATH

        # 安装Provisioning Profile
        mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
        cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/

        # 显示已安装的证书
        security find-identity -v -p codesigning

    - name: Build and Archive
      env:
        TEAM_ID: ${{ secrets.TEAM_ID }}
      run: |
        xcodebuild clean archive \
          -project AIPhotoClassifier.xcodeproj \
          -scheme AIPhotoClassifier \
          -archivePath $RUNNER_TEMP/AIPhotoClassifier.xcarchive \
          -sdk iphoneos \
          -configuration Release \
          -destination 'generic/platform=iOS' \
          CODE_SIGN_STYLE=Manual \
          DEVELOPMENT_TEAM=$TEAM_ID \
          CODE_SIGN_IDENTITY="Apple Development" \
          PROVISIONING_PROFILE_SPECIFIER=""

    - name: Export IPA
      run: |
        # 创建ExportOptions.plist
        cat > $RUNNER_TEMP/ExportOptions.plist << EOF
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>method</key>
            <string>development</string>
            <key>teamID</key>
            <string>${{ secrets.TEAM_ID }}</string>
            <key>uploadBitcode</key>
            <false/>
            <key>compileBitcode</key>
            <false/>
            <key>signingStyle</key>
            <string>manual</string>
            <key>signingCertificate</key>
            <string>Apple Development</string>
            <key>provisioningProfiles</key>
            <dict>
                <key>com.aiphoto.classifier</key>
                <string>match Development com.aiphoto.classifier</string>
            </dict>
        </dict>
        </plist>
        EOF

        # 导出IPA
        xcodebuild -exportArchive \
          -archivePath $RUNNER_TEMP/AIPhotoClassifier.xcarchive \
          -exportPath $RUNNER_TEMP/build \
          -exportOptionsPlist $RUNNER_TEMP/ExportOptions.plist

        # 移动到build目录
        mkdir -p build
        cp $RUNNER_TEMP/build/AIPhotoClassifier.ipa build/

    - name: Upload IPA
      uses: actions/upload-artifact@v4
      with:
        name: AIPhotoClassifier-IPA-Signed
        path: build/AIPhotoClassifier.ipa
        retention-days: 30

    - name: Upload Archive
      uses: actions/upload-artifact@v4
      with:
        name: AIPhotoClassifier-Archive-Signed
        path: ${{ runner.temp }}/AIPhotoClassifier.xcarchive
        retention-days: 30

    - name: Cleanup
      if: always()
      run: |
        # 删除临时钥匙串
        security delete-keychain $RUNNER_TEMP/app-signing.keychain-db || true
        # 删除临时文件
        rm -rf $RUNNER_TEMP/certificate.p12
        rm -rf $RUNNER_TEMP/profile.mobileprovision
        rm -rf ~/Library/MobileDevice/Provisioning\ Profiles/*.mobileprovision

    - name: Build Summary
      if: success()
      run: |
        echo "## 🎉 签名构建成功！" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "✅ **IPA已生成**: build/AIPhotoClassifier.ipa" >> $GITHUB_STEP_SUMMARY
        echo "✅ **签名类型**: Development" >> $GITHUB_STEP_SUMMARY
        echo "✅ **Team ID**: ${{ secrets.TEAM_ID }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📦 下载" >> $GITHUB_STEP_SUMMARY
        echo "在Artifacts中下载 **AIPhotoClassifier-IPA-Signed**" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📱 安装方法" >> $GITHUB_STEP_SUMMARY
        echo "1. 使用Xcode: Window → Devices → 拖入IPA" >> $GITHUB_STEP_SUMMARY
        echo "2. 使用AltStore/Sideloadly" >> $GITHUB_STEP_SUMMARY
        echo "3. 通过TestFlight分发" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        # 显示IPA文件信息
        if [ -f "build/AIPhotoClassifier.ipa" ]; then
          echo "### 📊 IPA信息" >> $GITHUB_STEP_SUMMARY
          ls -lh build/AIPhotoClassifier.ipa | awk '{print "**大小**: " $5}' >> $GITHUB_STEP_SUMMARY
        fi
